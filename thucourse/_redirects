# Cloudflare Pages Redirects
# Docs: https://developers.cloudflare.com/pages/configuration/redirects/
#
# ⚠️ Cloudflare Pages 使用前缀匹配（prefix match），且重定向会被持续跟踪。
#    单段路径如 /about 会匹配 /about.html，导致 301 死循环！
#    因此清洁 URL 由 404.html 中的 JS 处理。
#
# 只有两段式路径（如 /course/12345）不会与 /course.html 产生前缀冲突

# 课程详情短链接: /course/12345 → /course.html?sqid=12345
/thucourse/course/:sqid   /thucourse/course.html?sqid=:sqid   301
